<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="line">
    <!-- 破折线的宽度为dashWith，破折线之间的空隙的宽度为dashGap -->
    <stroke
        android:width="1dp"
        android:color="@color/customer_read_card_divide_line"
        android:dashGap="3dp"
        android:dashWidth="6dp" />
    <!-- 虚线的高度 -->
    <size android:height="1dp" />
</shape>