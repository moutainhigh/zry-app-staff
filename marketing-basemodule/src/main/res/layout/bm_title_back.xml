<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center_horizontal"
        android:id="@+id/ll_title_back"
        android:background="@drawable/bm_dinner_table_info_button_selector"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/tv_back_module"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="center"
            android:focusable="false"
            android:scaleType="center"
            android:src="@drawable/bm_title_back" />

        <!--
        <TextView
            android:focusable="false"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:id="@+id/tv_back_module_text"
            android:drawableLeft="@drawable/title_back"
            android:textSize="@dimen/text_20"
            android:textColor="@color/bg_white"
            android:gravity="center"
            android:layout_gravity="center"
            android:singleLine="true"
            android:ellipsize="end"/>
            -->
    </LinearLayout>
</merge>